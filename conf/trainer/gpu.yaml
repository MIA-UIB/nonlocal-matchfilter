_target_: lightning.Trainer

accelerator: gpu
devices:
  - 0
precision: 32-true # bf16-mixed
max_epochs: 500

# gradient_clip_algorithm: norm
# gradient_clip_val: 1.0

log_every_n_steps: 1

deterministic: False
# profiler:
#   _target_: lightning.pytorch.profilers.PyTorchProfiler
#   filename: "${now:%Y-%m-%d-%H-%M-%S}_profile"
#   row_limit: -1
